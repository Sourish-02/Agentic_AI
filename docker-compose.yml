services:
  data-pipeline-container:
    build: .
    container_name: data-pipeline-container
    env_file:
      - .env
    stdin_open: true
    ports:
      - "5001:5000"
    tty: true
    networks:
      - agents-net

networks:
  agents-net:
    external: true
